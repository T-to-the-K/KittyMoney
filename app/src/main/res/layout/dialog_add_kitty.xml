<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/etName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Kitty name (e.g. Family Fund)" />

    <EditText
        android:id="@+id/etTotalAmount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Monthly pot paid to the collector (optional)"
        android:inputType="numberDecimal"
        android:layout_marginTop="12dp" />

    <EditText
        android:id="@+id/etDuration"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Months it runs (optional, auto = one per member/slot)"
        android:inputType="number"
        android:layout_marginTop="12dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="The pot is what the collector receives each month. It sets each member's payment: full share pays (pot ÷ total shares), half pays half — the app enforces it. Leave blank for any-amount mode. Shares still apply (2 = double slot, 0.5 = half)."
        android:textSize="12sp"
        android:textColor="@color/warm_subtext" />

</LinearLayout>